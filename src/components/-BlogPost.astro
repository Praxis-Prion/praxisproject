---
// /sites/praxisproject/src/components/BlogPost.astro
// Receives url and title props from parent, renders a linked list item
// import Card from '../components/Card.astro';
import { Image } from 'astro:assets';
const { url, title, description, image, pubDate } = Astro.props;

---
<!-- <Card linkCard={true}>
  <a href={url}>
    {image && (
      <Image
        src={image.url}
        alt={image.alt}
        style="width: 100%; height: 200px; object-fit: cover;"
      />
    )}
    {pubDate && <span>{pubDate.toLocaleDateString()}</span>}
    <h3>{title}</h3>
    {description && <p>{description}</p>}
  </a>
</Card> -->